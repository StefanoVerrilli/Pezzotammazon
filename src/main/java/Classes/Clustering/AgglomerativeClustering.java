package Classes.Clustering;

import java.util.List;

public class AgglomerativeClustering extends Clustering {
    public AgglomerativeClustering(DistanceMetric metric) {
        super(metric);
    }
}
